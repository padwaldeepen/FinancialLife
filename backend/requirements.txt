fastapi==0.138.1
uvicorn[standard]==0.49.0
# sqlalchemy: Alembic's async migration runner only (env.py uses async_engine_from_config).
# Never imported in routers/ or services/ — all application queries are raw asyncpg SQL.
# See rules/database.md.
sqlalchemy[asyncio]==2.0.51
alembic==1.18.4
asyncpg==0.30.0
python-multipart==0.0.32
PyJWT==2.13.0
bcrypt==5.0.0
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0
httpx==0.28.1
# S2 tier C (no-Ollama, no-cloud-opt-in extraction path): local OCR only, zero network calls.
pytesseract==0.3.13
Pillow==11.0.0
# Text-layer PDF extraction (statements/bills are almost always digitally generated,
# not scanned) — pure Python, no system binary, no OCR needed for these.
pdfplumber==0.11.7
